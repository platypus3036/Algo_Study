# 📝 251107 임시 메모
### 잃어버린 괄호
- '-'가 한번이라도 나온 시점 이후로, 모든 부호를 '-'으로 계산하면 해결
- 처음에 0이 나오는 경우를 따로 계산해줘야함

- 탐색 기준을 부호가 아닌 일반 index 탐색과 같은 순서로 탐색해야 좀 더 효율적이라고 생각

- '-'를 기준으로 숫자를 여러 덩어리로 나누고, 첫번째 숫자에서 나머지 덩어리를 모두 빼는 계산과 구분
- 
### 최소 힙
- heapq를 사용하면 바로 풀리는 문제
- heapq를 사용하지 않는 방법으로는 list를 정렬해서 pop()으로 하려고 했으나, 실패
- int(input())을 자주 사용했는데, 시간이 더 오래걸려서 백준문제에서는 sys.stdin.readline()을 사용하는것이 유리하다고 함

- java에서는 우선순위q가 pq로 있어서, 바로 사용해서 해결
- heapq는 root node의 최솟값만 보장이 되고, 나머지에서는 보장 x

- heapq.heappush(heap, x)는 힙에 추가하는 함수,
- heapq.heappop(heap)은 가장 작은 값을 출력, 배열에서 제거
- 
### 로고
- 사각형이 다른 사각형 안에 왼전히 들어가 있는 경우 연결되지 않는 것으로 쳐야함
- 두 사각형이 완전히 분리되었는지를 체크하는 함수와, 두 사각형이 완전한 포함관계인지를 체크하는 함수 총 2개를 만들어서 사용
- kruskal 알고리즘을 이용해서 풀어보고자 함

- dfs를 이용해서 풀이하는 방법도 있으나, 너무 어려워보임
